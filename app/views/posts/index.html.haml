- title = posts_title(@tag)
- content_for(:head, auto_discovery_link_tag(:atom, @tag.nil? ? formatted_posts_path(:format => 'atom') : posts_path(:tag => @tag, :format => 'atom')))

- @posts.each do |post|
  %article{:id => "post-#{post.id}", :class => :post}
    = render :partial => 'post', :locals => {:post => post, :content => :short_body_html}
  - if more_content?
    .related Looking for more? Head on over to the #{link_to("archives", archives_path)}.
